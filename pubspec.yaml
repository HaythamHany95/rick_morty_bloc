name: rick_morty_bloc
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.2.0
  dio: ^5.2.1
  either_dart: ^0.2.2
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.4.5
  hive_generator: ^1.1.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/